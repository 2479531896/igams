<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.matridx.igams.wechat.dao.post.IKhhtglDao">

    <!--新增所有列-->
    <insert id="insert" parameterType="KhhtglDto">
        insert into igams_khhtgl(khid,htid,xh)
        values (#{khid},#{htid},(select cast(coalesce(max(xh),'0') as numeric) +1 from igams_khhtgl where khid = #{khid}))
    </insert>

    <insert id="insertList" parameterType="list">
        insert into igams_khhtgl(khid,htid,xh)
        values
        <foreach collection="list" item="item" separator="," index="index">
            (#{item.khid},#{item.htid},(select cast(coalesce(max(xh),'0') as numeric) +1 + #{index} from igams_khhtgl where khid = #{item.khid}))
        </foreach>
    </insert>

    <!--通过主键删除-->
    <delete id="delete" parameterType="KhhtglDto">
        delete from igams_khhtgl where
        <if test="ids !=null and ids.size > 0">
            htid in
            <foreach collection="ids" open="(" close=")" separator="," item="item">
                #{item}
            </foreach>
        </if>
        <if test="htid !=null and htid != ''">
            htid = #{htid}
        </if>
    </delete>

</mapper>

