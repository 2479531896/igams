<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.matridx.las.netty.dao.post.IMlpzDao" >

    <select id="getMlpzList" parameterType="MlpzDto" resultType="MlpzDto">
        select * from matridx_mlpz where sjid=#{sjid} and scbj !='1'  ORDER BY xh asc
    </select>

    <select id="getDtoById" parameterType="String" resultType="MlpzDto">
        select * from matridx_mlpz where mlid=#{mlid} and scbj !='1'
    </select>
</mapper>
