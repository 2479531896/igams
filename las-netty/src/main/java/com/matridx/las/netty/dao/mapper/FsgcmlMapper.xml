<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.matridx.las.netty.dao.post.IFsgcmlDao">
	<select id="getFsgcmlDtosByType" parameterType="String"
		resultType="FsgcmlDto">
		select * from matridx_fsgcml where gclx=#{type} and scbj!='1'
		ORDER BY xh asc
	</select>
	
	<select id="getDtoById" parameterType="String"
		resultType="FsgcmlDto">
		select * from matridx_fsgcml where gcid=#{id} and scbj!='1'
	</select>
</mapper>
