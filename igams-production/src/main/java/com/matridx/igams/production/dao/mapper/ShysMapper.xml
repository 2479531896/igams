<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.matridx.igams.production.dao.post.IShysDao">
    <insert id="insertDtoList" parameterType="List">
        insert into igams_shys(id,autoid,shys,lrsj,lrry)
        values
        <if test="list !=null and list.size > 0">
            <foreach collection="list" separator="," item="item" index="index">
                (
                #{item.id},#{item.autoid},cast(#{item.shys} as json),LOCALTIMESTAMP,#{item.lrry}
                )
            </foreach>
        </if>
    </insert>

    <update id="updateDispose" parameterType="ShysDto">
        update igams_shys set cszt=#{cszt},csbz=#{csbz}
        where id=#{id} and autoid=#{autoid}
    </update>
</mapper>