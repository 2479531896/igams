<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.matridx.igams.common.dao.post.ISylcmxDao" >

    <select id="getDtoList" parameterType="SylcmxDto" resultType="SylcmxDto">
        select sylcmx.lcmxid ,sylcmx.lcid ,sylcmx.xsmc ,sylcmx.zyid ,sylcmx.xh ,sylcmx.tzcs,xtzy_cd.zybt,czdm.czmc,sylcmx.mkid,xtzy_mk.zybt as mkmc
        from igams_sylcmx sylcmx
        left outer join matridx_xtzy xtzy_mk on xtzy_mk.zyid = sylcmx.mkid
        left outer join matridx_xtzy xtzy_cd on xtzy_cd.zyid = sylcmx.zyid
        left outer join matridx_czdm czdm on czdm.czdm = sylcmx.tzcs
        <where>
            sylcmx.scbj='0'
            <if test="lcid !=null and lcid != ''">
                and sylcmx.lcid = #{lcid}
            </if>
        </where>
        order by cast(sylcmx.xh as numeric) asc
    </select>

    <insert id="insertList" parameterType="List">
        insert into igams_sylcmx(lcmxid,lcid,xsmc,zyid,xh,tzcs,mkid,lrry,lrsj,scbj)
        values
        <if test="list !=null and list.size > 0">
            <foreach collection="list" separator="," item="item" index="index">
                (
                #{item.lcmxid},#{item.lcid},#{item.xsmc},#{item.zyid},#{item.xh},#{item.tzcs},#{item.mkid},#{item.lrry},LOCALTIMESTAMP,'0'
                )
            </foreach>
        </if>
    </insert>

    <delete id="delete" parameterType="SylcmxDto">
        update igams_sylcmx set scry = #{scry},scsj = LOCALTIMESTAMP,scbj = '1'
        where lcid in
        <foreach collection="ids" open="(" close=")" index="index" item="item" separator=",">
            #{item}
        </foreach>
    </delete>

    <delete id="delObsoleteData" parameterType="SylcmxDto">
        delete from  igams_sylcmx
        where scbj='1' and lcid in
        <foreach collection="ids" open="(" close=")" index="index" item="item" separator=",">
            #{item}
        </foreach>
    </delete>
</mapper>
