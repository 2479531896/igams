<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.matridx.igams.detection.molecule.dao.post.IFzzkjgDao" >

    <insert id="insertList" parameterType="List">
        insert into igams_fzzkjg(fzzkjgid,
        zkmc,
        glid,
        ctz,
        jgid
        )
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.fzzkjgid},#{item.zkmc},#{item.glid},#{item.ctz},#{item.jgid})
        </foreach>
    </insert>

    <select id="getDtoList" parameterType="FzzkjgDto" resultType="FzzkjgDto">
        select
        fzzkjgid,
        zkmc,
        glid,
        ctz,
        jgid
        from igams_fzzkjg
        where glid=#{glid}
    </select>
</mapper>
